<Window x:Class="DriverGuardian.UI.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Localized.AppTitle}" Height="420" Width="760">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding Localized.MainHeader}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12">
            <Button Width="220" Height="34" Command="{Binding StartScanCommand}" Content="{Binding Localized.ScanButton}" />
            <TextBlock Margin="16,8,0,0" Text="{Binding StatusText}" />
        </StackPanel>

        <Border Grid.Row="2" Padding="12" BorderThickness="1" CornerRadius="6" BorderBrush="#DDD">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="{Binding Localized.ResultsHeader}" Margin="0,0,0,8" />
                <TextBlock Text="{Binding ResultsPlaceholder}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
