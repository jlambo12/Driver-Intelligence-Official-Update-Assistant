<UserControl x:Class="DriverGuardian.UI.Wpf.Views.Sections.HistorySectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:localization="clr-namespace:DriverGuardian.UI.Wpf.Localization">
    <Border Style="{StaticResource SecondaryCardStyle}">
        <StackPanel>
            <TextBlock Text="{x:Static localization:UiStrings.RecentHistorySectionTitle}" FontWeight="SemiBold" Margin="0 0 0 4"/>
            <TextBlock Text="{x:Static localization:UiStrings.RecentHistorySectionHint}" Margin="0 0 0 6" TextWrapping="Wrap"/>
            <ItemsControl ItemsSource="{Binding RecentHistory}">
                <ItemsControl.Style>
                    <Style TargetType="ItemsControl">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RecentHistory.Count}" Value="0">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.Style>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#D6E2F1" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0 0 0 6" Background="#F8FAFC">
                            <StackPanel>
                                <DockPanel LastChildFill="False" Margin="0 0 0 2">
                                    <TextBlock DockPanel.Dock="Left" FontWeight="SemiBold" Text="{Binding Title}" TextWrapping="Wrap" Foreground="#0F172A"/>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding Timestamp}" Foreground="#64748B" FontSize="11" Margin="8 0 0 0"/>
                                </DockPanel>
                                <TextBlock Text="{Binding Summary}" Margin="0 2 0 0" TextWrapping="Wrap" Foreground="#334155"/>
                                <TextBlock Text="{Binding Status}" Margin="0 3 0 0" TextWrapping="Wrap" FontWeight="SemiBold" Foreground="#1E3A8A"/>
                                <Border BorderBrush="#DBEAFE" BorderThickness="1" CornerRadius="4" Padding="6" Margin="0 4 0 0" Background="#EFF6FF">
                                    <TextBlock Text="{Binding Guidance}" TextWrapping="Wrap" Foreground="#1E293B"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="{x:Static localization:UiStrings.RecentHistoryEmptyState}" TextWrapping="Wrap">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RecentHistory.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>
    </Border>
</UserControl>
