<UserControl x:Class="DriverGuardian.UI.Wpf.Views.Sections.ManualActionSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <Border Style="{StaticResource PrimaryWorkflowCardStyle}" Background="#FCFDFF" Margin="0 0 0 10">
            <StackPanel>
                <DockPanel LastChildFill="True" Margin="0 0 0 6">
                    <Border DockPanel.Dock="Left" Width="22" Height="22" CornerRadius="11" Background="#E0E7FF" Margin="0 0 8 0" VerticalAlignment="Center">
                        <TextBlock Text="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12" FontWeight="SemiBold" Foreground="#1E3A8A"/>
                    </Border>
                    <TextBlock Text="{Binding Results.ManualSectionTitle}" FontWeight="SemiBold" FontSize="16" Foreground="#1E3A8A"/>
                </DockPanel>
                <TextBlock Text="{Binding Results.ManualSectionHint}" Margin="0 0 0 8" TextWrapping="Wrap" Foreground="#1E293B"/>
                <ItemsControl ItemsSource="{Binding Results.UserGuidedSteps}">
                    <ItemsControl.Style>
                        <Style TargetType="ItemsControl">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Results.UserGuidedSteps.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.Style>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource PrimaryWorkflowInnerCardStyle}" Padding="8" Margin="0 0 0 6">
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" TextWrapping="Wrap">
                                        <Run Text="• " />
                                        <Run Text="{Binding Title}" />
                                        <Run Text=" — " />
                                        <Run Text="{Binding Status}" />
                                    </TextBlock>
                                    <TextBlock Text="{Binding Description}" Margin="0 4 0 0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Border Style="{StaticResource PrimaryWorkflowInnerCardStyleWhenUserGuidedStepsEmptyVisible}" Padding="10">
                    <TextBlock Text="{Binding Results.ManualSectionEmptyState}" TextWrapping="Wrap" Foreground="#334155"/>
                </Border>
            </StackPanel>
        </Border>

        <TextBlock Text="↓" FontSize="16" Foreground="#64748B" HorizontalAlignment="Center" Margin="0 0 0 8"/>
    </StackPanel>
</UserControl>
